# `quant_lib.tools`

Composable white-box public API: data fetching, features,
backtesting (walk-forward, trade loop), portfolio simulation,
statistics (SPA, PSR, FDR), universe selection.

Sprint 2 fix: rendered members = summary (signatures + docstrings) so
the API reference is discoverable. This is the recommended public API
for users building custom pipelines.

::: quant_lib.tools
    options:
      show_root_heading: false
      show_source: false
      members: summary
      docstring_style: numpy
